# Backend API for PTO Tracker

Simple Express.js backend for the PTO Tracker application.

## Setup

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Server runs on http://localhost:5000